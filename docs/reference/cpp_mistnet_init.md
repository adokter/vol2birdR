# Initializes the mistnet shared library pointed to by the path

Initializes the mistnet shared library pointed to by the path

## Usage

``` r
cpp_mistnet_init(path)
```

## Arguments

- path:

  The shared library
